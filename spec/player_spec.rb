require 'player'
require 'spec_helper'

describe Player, "player creation" do
  let(:name) { "Tetto" }
  let(:race) { "Giant" }
  let(:sex) { "Male"}

  it "should not be able to create a player with a invalid name" do
    pending "write a test for player creation"
  end
end
